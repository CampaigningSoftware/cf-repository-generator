<?php

namespace %namespaces.factories%;

use Contentful\Delivery\DynamicEntry;
use %namespaces.models%\%modelName%;

class %modelName%Factory
{
    public static function build(DynamicEntry $entry): %modelName%
    {
        return new %modelName%(%modelGetterList%);
    }
}